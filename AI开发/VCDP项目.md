# 一、背景

该项目用于进行车辆服务编排，可以创建工程、ECU、总线、报文、信号信息，支持数据导入导出
# 二、框架

后端服务位于项目根目录的`./serve`目录下

使用SpringBoot框架，数据库使用内嵌式数据库H2，采用本地文件的持久化存储模式

数据库需要有schedule.sql文件定义dll数据库建表语句

使用mabatis的方式调用数据库，每个持久化对象都要配套有mapper接口和mapper.xml来定语sql语句

前端工程在项目根目录的`./ui`目录下

使用 VUE.js, 使用后端SpringBoot的内嵌式Tomcat来启动

需要在项目根目录下创建一个build.sh脚本，该脚本可以将前端工程自动打包到后端服务的静态资源目录，同时可以将后端服务打包成jar

# 三、开发流程
## 1. 项目工程页面

进入项目首页后，展示项目列表

工程以卡片网格形式展现，需要展现的字段有：名称、描述

项目很多时要支持分页查询

卡片上要有编辑、删除功能

工程列表页面需要有新增、批量删除

### 数据定义

Project

| 字段   | 类型     | 含义   | 备注     | 是否页面展示 | 是否必填校验 | 其他校验规则            |
| ---- | ------ | ---- | ------ | ------ | ------ | ----------------- |
| id   | Long   | 主键ID | 雪花算法生成 | 否      | 前端不可配置 |                   |
| name | String | 工程名称 |        | 是      | 必填     | 工程名称不可与数据库中存量工程重复 |
| desc | String | 描述   |        | 是      | 非必填    |                   |

## 2. ECU配置页面

点击进入工程后，左侧菜单要有ECU配置，点击后进入ECU列表页面

ECU列表页面，需要有新增、批量删除的功能

ECU也是网格化卡片展示的，显示名称name、款型type，卡片上需要有查看、编辑、删除的功能

点击新增、查看、编辑功能时都是个大弹窗，大弹窗里面有多个tab页，每个tab页都对应一种配置数据

1. ECU基础配置页

Ecu

| 字段        | 类型      | 含义      | 备注                                   | 是否页面展示 | 是否必填校验 | 其他校验规则                           |
| --------- | ------- | ------- | ------------------------------------ | ------ | ------ | -------------------------------- |
| id        | Long    | 主键ID    | 雪花算法生成                               | 否      | 前端不可配置 |                                  |
| projectId | Long    | 关联的工程ID | 创建ECU接口请求时必须带工程ID                    | 否      | 前端不可配置 | projectId必须能够在工程表中匹配查询到          |
| name      | String  | ECU设备名称 |                                      | 是      | 必填     | 同project工程下的ECU设备名称不可重复          |
| desc      | String  | 描述      |                                      | 是      | 非必填    |                                  |
| mac       | String  | MAC地址   | 传递到后端服务时不用带冒号，前端用户输入时自动展示冒号，不用输入冒号   | 是      | 必填     | mac地址要符合协议规范，同项目中的ECU的mac地址不可以重复 |
| ip        | String  | IP地址    | 传递到后端服务时需要带上分隔符.，前端用户输入时不用输入分隔符.自动展示 | 是      | 必填     | ip地址要符合协议规范，同项目中的ECU的ip地址不可以重复   |
| port      | Integer | 端口号     |                                      | 是      | 必填     | 需要复合协议规范，最起码应是是大于等于0的整数          |
| index     | Integer | 部件索引号   |                                      | 是      | 必填     | 必须是大于等于0的整数，且同项目中的ECU的index不可以重复 |

2. 转发表通信配置页

EcuForwardInfo

| 字段                       | 类型     | 含义             | 备注                                  | 是否页面展示 | 是否必填校验 | 其他校验规则                            |
| ------------------------ | ------ | -------------- | ----------------------------------- | ------ | ------ | --------------------------------- |
| id                       | Long   | 主键ID           | 雪花算法生成                              | 否      | 前端不可配置 |                                   |
| projectId                | Long   | 关联的工程ID        | 创建EcuForwardInfo时必须带工程ID、和EcuId     | 否      | 前端不可配置 | projectId必须能够在工程表中匹配查询到           |
| ecuId                    | Long   | 关联的ECU 设备ID    | 创建EcuForwardInfo接口请求时必须带工程ID、和EcuId | 否      | 前端不可配置 | ecuId必须能在当前工程下的ECU列表中匹配           |
| pFlashMemoryStartAddress | String | pFlash空间起始地址   | 用于S19编排的起始地址                        | 是      | 必填     | 大于0的十六进制数，并且要使用公共方法统一十六进制格式，即0x开头 |
| pFlashMemorySizeLimit    | String | pFlash空间编排大小限制 | 用于S19编排结果的大小限制校验                    | 是      | 必填     | 大于0的十六进制数，并且要使用公共方法统一十六进制格式，即0x开头 |
| ramMemoryStartAddress    | String | RAM空间起始地址      | 用于RAM空间S19编排的起始地址                   | 是      | 必填     | 大于0的十六进制数，并且要使用公共方法统一十六进制格式，即0x开头 |
| ramMemorySizeLimit       | String | RAM空间S19编排大小限制 | 用于S19必拿牌结果的大小限制校验                   | 是      | 必填     | 大于0的十六进制数，并且要使用公共方法统一十六进制格式，即0x开头 |

3. CAN接口配置页

CanInterface

| 字段            | 类型      | 含义                           | 备注                                                            | 是否页面展示 | 是否必填校验 | 其他校验规则                               |
| ------------- | ------- | ---------------------------- | ------------------------------------------------------------- | ------ | ------ | ------------------------------------ |
| id            | Long    | 主键ID                         | 雪花算法生成                                                        | 否      | 前端不可配置 |                                      |
| projectId     | Long    | 关联的工程ID                      | 创建CanInterface时必须带工程ID、和EcuId                                 | 否      | 前端不可配置 | projectId必须能够在工程表中匹配查询到              |
| ecuId         | Long    | 关联的ECU 设备ID                  | 创建CanInterface时必须带工程ID、和EcuId                                 | 否      | 前端不可配置 | ecuId必须能在当前工程下的ECU列表中匹配              |
| interfaceName | String  | CAN接口名称                      |                                                               | 是      | 必填     | 同工程同ECU下的CAN、LIN、ETH接口名称不可重复         |
| channelId     | Integer | 接口通道ID                       |                                                               | 是      | 必填     | 同工程同ECU下的CanInterface中的channelId不可重复 |
| type          | Integer | 接口类型：0-CAN，1-CANFD           | 前端需要进行下拉框选取，展示的是类型的字符串名称，传给后端时是数字。后端需要创建枚举，前端需要通过接口查询接口类型枚举列表 | 是      | 必填     | 必须在枚举定义范围内                           |
| connType      | Integer | 接口连接类型：0-MCU直连CAN，1-LSW下挂CAN | 前端需要进行下拉框选取，展示的是类型的字符串名称，传给后端时是数字。后端需要创建枚举，前端需要通过接口查询接口类型枚举列表 | 是      | 必填     | 必须在枚举定义范围内                           |

4. Lin接口配置页

LinInterface

| 字段        | 类型   | 含义          | 备注                            | 是否页面展示 | 是否必填校验 | 其他校验规则                  |
| --------- | ---- | ----------- | ----------------------------- | ------ | ------ | ----------------------- |
| id        | Long | 主键ID        | 雪花算法生成                        | 否      | 前端不可配置 |                         |
| projectId | Long | 关联的工程ID     | 创建LinInterface时必须带工程ID、和EcuId | 否      | 前端不可配置 | projectId必须能够在工程表中匹配查询到 |
| ecuId     | Long | 关联的ECU 设备ID | 创建CanInterface时必须带工程ID、和EcuId | 否      | 前端不可配置 | ecuId必须能在当前工程下的ECU列表中匹配 |
|           |      |             |                               |        |        |                         |

	1. id，主键id，不可配置，不用前端展示，自动雪花算法生成
	2. peojectId，不可配置，不用前端展示，自关联工程
	3. ecuId，不可配置，不用前端展示，自动关联ecu
	4. interfaceName，接口名称，必填
	5. channelId，接口id，必填，从0开始的整数
5. Eth接口配置，EthInterface：
	1. id，主键id ，不可配置，不用前端展示，自动雪花算法生成
	2. peojectId，不可配置，不用前端展示，自动关联工程
	3. ecuId，不可配置，不用前端展示，自动关联ecu
	4. interfaceName，接口名称，必填
	5. type，必填，0-百兆口，1-千兆口，需要生成枚举，下拉选择

ECU基础配置，转发表通信配置，这两个标签页，是固定配置输入窗口，其他接口配置标签页中需要有新增接口的按钮，新增后生成一行配置需要用户输入

最终在这个大弹窗的右下角有保存、和取消按钮，保存时需要校验所有必填项目，并一次性创建所有配置并自动关联

## 3. CAN/LIN通信配置

进入工程后，左侧菜单需要有 `CAN/LIN通信` 配置按钮

总线配置页面也需要有新增、批量删除的功能

总线以列表形式展现，每列都有编辑、删除的按钮功能

新增、编辑时，可在 新增列/当前列 展示输入框，然后输入必填配置后点击保存按钮进行数据入库

配置项和实例对象的定义：成
- id， 主键id，不可配置，无需前端展示，雪花算法自动生成
- subnet，String ，总线名称，必填
- interface，接口信息，下拉框选择，显示（`<ECU名称>:<CAN/LIN接口名称>`），下拉框的可选内容是通过后端接口查询的，将LinInterface和CanInterface列表查出来，然后用户进行选择之后，自动填充如下内容
	- busId，String，不可配置，